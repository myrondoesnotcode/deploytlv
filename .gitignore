# Sensitive raw inputs — NEVER commit (GitHub Pages serves everything publicly)
build/source/
*_chat.txt
WhatsApp Chat*.zip

# Build data containing attendee contact PII (emails/phones). The pipeline lives
# in build/deploy3/ but its source data must stay local (see build/deploy3/README.md).
build/deploy3/deploy3-profiles.json
exports/

# PLAINTEXT attendee data — must stay local. Only the ENCRYPTED data.enc.js is
# committed/served (generated by build/encrypt-portal.js). See "Security" in CLAUDE.md.
events/1/data.js
events/2/data1.js
events/2/data2.js
events/3/data.js

# OS
.DS_Store
**/.DS_Store

# Pipeline working files with raw chat handles / phone numbers — keep local
build/pulse-candidates.md
build/roster-gaps.md
build/deploy3/li_data.txt
